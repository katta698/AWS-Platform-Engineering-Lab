# Capture slots declared but not published, with the reason.
# A number is never recycled; a capture not worth publishing is retired here.

# 07 - the catch-all rule recording what the specific rule ignored.
#      Covered by 06, which shows the matched path delivering and the catch-all
#      counting the unmatched event in the same run, on the same evidence.
07-detection-catches-it.png

# 09 - a real failed delivery in the DLQ.
#      Covered by 06 as well: the same run shows the Lambda on-failure queue at
#      1 -> 2 while the EventBridge DLQ stays at 0, which is the actual finding.
09-dlq-caught-a-failure.png

# 10 - Cost Explorer for the run window.
#      Pending: the bill posts ~25 Sep. Read from Cost Explorer, never a rate card.
10-cost-explorer.png
